# Audit 71–80 — Game Profile & Session Layer — LogiMate 0.2.0-alpha

71. Versioned per-game profile schema with safe defaults.
72. Native running-process detection without PowerShell.
73. Optional foreground-only profile gate.
74. Safe Auto-Apply changes only Wheel-Engine profile/range; it never starts FFB.
75. Per-game gain overrides are bounded by the per-wheel safety layer.
76. LED policy is stored per game profile.
77. Telemetry-adapter binding is part of each game profile.
78. JSON import/export is size-limited, normalized and validated.
79. Explicit game-session runtime state is exposed for UI and diagnostics.
80. Regression guard: game-profile activation cannot auto-start motor force.
